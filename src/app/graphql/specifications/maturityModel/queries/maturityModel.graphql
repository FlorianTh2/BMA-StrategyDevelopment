query MaturityModel($maturityModelId: ID!){
  maturityModel(id: $maturityModelId){
    name
    version
    partialModels {
      name
      weight
      subPartialModels{
        name
        weight
      }
    }
  }
}

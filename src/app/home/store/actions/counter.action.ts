import { createAction } from "@ngrx/store";

export const increment = createAction("[Home] Increment");
export const decrement = createAction("[Home] Decrement");
export const reset = createAction("[Home] Reset");
